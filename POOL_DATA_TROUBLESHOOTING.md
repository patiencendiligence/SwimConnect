# 🏊‍♂️ 수영장 데이터 표시 문제 해결 가이드

## 문제: "수영장이 표시되지 않습니다"

### ✅ 해결 완료된 사항

1. **거리 제한 제거**
   - 이전: `DEFAULT_SEARCH_RADIUS` (기본 5km) 내의 수영장만 표시
   - 개선: 모든 수영장을 표시하고 거리순으로 정렬만 함

2. **상세 로그 추가**
   - 데이터 초기화 과정
   - Firestore에서 데이터 로드
   - 필터링 및 정렬 과정

3. **샘플 데이터 자동 추가**
   - 앱 최초 실행 시 서울 지역 10개 수영장 자동 추가
   - 중복 추가 방지 로직 포함

---

## 📋 체크리스트

### 1. Firebase 설정 확인

```bash
# firebase.config.ts 파일이 있는지 확인
ls -la src/firebase.config.ts
```

**파일이 없는 경우:**
1. `src/firebase.config.example.ts`를 복사
2. `src/firebase.config.ts`로 이름 변경
3. Firebase 콘솔에서 프로젝트 설정 가져오기
4. 설정 값 입력

### 2. 로그 확인

앱을 실행하고 수영장 탭으로 이동한 후, 터미널에서 다음과 같은 로그를 확인하세요:

**정상적인 로그 순서:**
```
📍 샘플 수영장 데이터 초기화 시작...
🔍 수영장 데이터 확인 중...

[첫 실행인 경우]
📝 샘플 수영장 10개 추가 시작...
  1. 올림픽수영장 - 서울특별시 송파구 올림픽로 424
  2. 잠실 실내수영장 - 서울특별시 송파구 올림픽로 240
  ...
🎉 10개의 수영장 데이터가 성공적으로 추가되었습니다!

[이미 데이터가 있는 경우]
✅ 수영장 데이터가 이미 존재합니다. (초기화 건너뛰기)

✅ 샘플 데이터 초기화 완료
📥 수영장 데이터 로드 시작...
🔍 Firestore에서 수영장 데이터 가져오는 중...
📦 Firestore에서 10개의 문서를 가져왔습니다.
✅ 수영장 10개 로드 완료: ["올림픽수영장", "잠실 실내수영장", ...]
🏊 총 수영장 수: 10, 사용자 위치: 있음/없음
🔍 필터 적용 후: 10개
📏 거리순 정렬 완료: 10개
```

### 3. 에러 로그 확인

**에러가 발생한 경우 확인할 로그:**

```
❌ 데이터 초기화 실패: [에러 메시지]
❌ 수영장 로드 실패: [에러 메시지]
❌ 샘플 데이터 초기화 실패: [에러 메시지]
```

**일반적인 에러 원인:**

1. **Firebase 설정 누락**
   - 에러: `[firebase/app] Firebase: No Firebase App '[DEFAULT]'`
   - 해결: `firebase.config.ts` 파일 설정

2. **Firestore 권한 문제**
   - 에러: `PERMISSION_DENIED: Missing or insufficient permissions`
   - 해결: Firebase Console > Firestore Database > 규칙
   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /{document=**} {
         allow read, write: if request.auth != null;
       }
     }
   }
   ```

3. **네트워크 연결 문제**
   - 에러: `Network request failed`
   - 해결: 인터넷 연결 확인

### 4. 위치 권한 확인

**Android:**
```xml
<!-- android/app/src/main/AndroidManifest.xml -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
```

**iOS:**
```xml
<!-- ios/SwimConnect/Info.plist -->
<key>NSLocationWhenInUseUsageDescription</key>
<string>주변 수영장을 찾기 위해 위치 정보가 필요합니다.</string>
```

### 5. 데이터 강제 재초기화

만약 데이터가 이상하거나 재초기화가 필요한 경우:

**방법 1: Firebase Console 사용**
1. Firebase Console 접속
2. Firestore Database 이동
3. `pools` 컬렉션 삭제
4. 앱 재시작

**방법 2: 코드로 강제 초기화**

`src/services/firestore.ts`에서 임시로 수정:
```typescript
// 이 부분을 주석 처리
if (!existingPools.empty) {
  console.log('✅ 수영장 데이터가 이미 존재합니다. (초기화 건너뛰기)');
  // return; // 이 줄을 주석 처리하면 강제로 재추가됨
}
```

---

## 🔍 디버깅 팁

### Metro Bundler 로그 보기
```bash
cd /Users/a20240302/dev/SwimConnect
npx react-native start
```

### Android 로그 보기
```bash
adb logcat | grep -i "firestore\|pool\|수영장"
```

### iOS 로그 보기
```bash
# Xcode에서 Console 창 확인
# 또는
xcrun simctl spawn booted log stream --predicate 'processImagePath contains "SwimConnect"'
```

---

## 📞 추가 지원

위의 모든 단계를 수행했는데도 문제가 해결되지 않는 경우:

1. **React Native Debugger 사용**
   - Chrome DevTools 열기: 앱에서 Cmd+D (iOS) 또는 Cmd+M (Android)
   - "Debug" 선택
   - Console에서 로그 확인

2. **Firestore 콘솔 직접 확인**
   - Firebase Console 접속
   - Firestore Database 이동
   - `pools` 컬렉션에 10개의 문서가 있는지 확인

3. **데이터 구조 확인**
   - 각 문서에 필수 필드가 있는지 확인:
     - name
     - address
     - latitude
     - longitude
     - price
     - rating

---

## ✨ 개선 사항

- ✅ 거리 제한 제거로 모든 수영장 표시
- ✅ 상세 로그로 문제 진단 용이
- ✅ 자동 샘플 데이터 추가
- ✅ 위치 정보 없어도 수영장 목록 표시
- ✅ 거리순 정렬로 가까운 수영장부터 표시

